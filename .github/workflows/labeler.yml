name: "Labeler"
on:
- pull_request

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@v2
      if: ${{ env.GITHUB_HEAD_REF == "" }}
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
